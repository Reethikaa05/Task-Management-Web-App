import React from 'react';import { Task } from '../types';import { computeROI, formatROI } from '../utils';type Props={task:Task;onView:(t:Task)=>void;onEdit:(t:Task)=>void;onDelete:(t:Task)=>void};export default function TaskRow({task,onView,onEdit,onDelete}:Props){const roi=computeROI(task);return <div className='task-row' onClick={()=>onView(task)}><div><div><strong>{task.title}</strong></div><div className='task-meta'>Revenue: ₹{task.revenue} • Time: {task.timeTaken}h • ROI: {formatROI(roi)}</div></div><div className='actions' onClick={(e)=> e.stopPropagation()}><button onClick={()=>onEdit(task)}>Edit</button><button onClick={()=>onDelete(task)}>Delete</button></div></div>}