import React, { useEffect } from 'react';type Props={open:boolean;message:string;actionLabel?:string;onAction?:()=>void;onClose:()=>void};export default function Snackbar({open,message,actionLabel='Undo',onAction,onClose}:Props){useEffect(()=>{ if(!open) return; const id=setTimeout(()=> onClose(),5000); return ()=> clearTimeout(id) },[open,onClose]); if(!open) return null; return (<div className='snackbar'><div>{message}</div>{onAction && <button onClick={onAction}>{actionLabel}</button>}<button onClick={onClose} style={{marginLeft:8}}>Close</button></div>)}