import React from 'react';import { Task } from '../types';import { computeROI, formatROI } from '../utils';type Props={task:Task;onClose:()=>void};export default function ViewDialog({task,onClose}:Props){return (<><div className='overlay' onClick={onClose}></div><div className='dialog'><h3>{task.title}</h3><div>Revenue: ₹{task.revenue}</div><div>Time Taken: {task.timeTaken}h</div><div>ROI: {formatROI(computeROI(task))}</div><div>Priority: {task.priority}</div><div style={{marginTop:8}}><button onClick={onClose}>Close</button></div></div></>)}